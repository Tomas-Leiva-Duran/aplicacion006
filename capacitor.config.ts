import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.aplicacion006',
  appName: 'aplicacion006',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
